import React from 'react';
import LockSymbol from './lock-symbol';

const LockedBlockChat = () => (
    <div className='locked-block'>
        <LockSymbol />
    </div>
);

export default LockedBlockChat;
